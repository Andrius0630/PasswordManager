-- CREATE TABLE creds(userid INTEGER NOT NULL PRIMARY KEY, username TEXT NOT NULL, userpass NOT NULL);

-- INSERT INTO creds(userid, username, userpass)
-- VALUES (0, "admin", "admin")

SELECT * FROM creds;